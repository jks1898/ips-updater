name: Update api.txt

on:
  schedule:
    # GitHub 使用 UTC 时间，北京时间比 UTC 快 8 小时
    # 所以要想在北京时间每小时整点执行，UTC 应该提前 8 小时执行
    # 即 '0 * * * *' UTC == 北京时间每小时 +8h
    - cron: '0 * * * *'  # ✅ 无需改动，就是北京时间整点
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'  # 升级到最新稳定版

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run IP collection script
        run: python collect_api.py

      - name: Commit and push api.txt if changed
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout main

          if ! git diff --quiet api.txt; then
              git add api.txt
              git commit -m "Update CT api.txt"
              git pull --rebase origin main || true
              git push origin main || git push origin HEAD:main --force-with-lease
          fi

      - name: Debug info
        if: ${{ always() }}
        run: |
          echo "UTC time: $(date -u)"
          echo "Beijing time: $(date -d '+8 hour')"
          echo "Branch: $(git branch --show-current)"
